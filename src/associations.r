# Find the genes associated with a particular disease.

bpa <- rep(0L, dim(bio_pathways_associations)[1])


# for (i in 1:dim(bio_pathways_associations)[1])
# {
#   bpa[i] <- c(bio_pathways_associations[1][i])
# }


bio_pathways_associations[3][1]
